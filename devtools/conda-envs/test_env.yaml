name: test

channels:
  - conda-forge
  - default

dependencies:
  # Base depends
  - python
  - pip

  # Testing
  - black
  - codecov
  - flake8
  - jinja2
  - pytest
  - pytest-cov

  # Documentation
  - pygments

  # Pip-only installs
  - pip:
    # Documentation
    - rinohtype
    - sphinx-rtd-theme
    - pystemmer
